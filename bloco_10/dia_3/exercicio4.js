const upperCase = (str) => {
    return str.toUpperCase()
}

const firstLetter = (str) => {
    return str[0]
}

const joinStrings = (str1, str2) => {
    return str1 + str2
}

module.exports = {
    upperCase,
    firstLetter,
    joinStrings,
}